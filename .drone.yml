pipeline:
  build:
    image: python:${PYTHON_VERSION}
    when:
      event: deployment
      environment: staging

    commands:
        - echo ${TEST}
        - pip install -r requirements.txt
        - python setup.py install

matrix:
  PYTHON_VERSION:
    - 3.4
    - 3.5
    - 3.6
